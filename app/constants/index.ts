import measures from './measures';
import font from './font';
import flags from './flags';
import filters from './filterOptions';
import globalProject from './globalProjects';

export const C = {
  measures,
  font,
  flags,
  filters,
  globalProject,
};
