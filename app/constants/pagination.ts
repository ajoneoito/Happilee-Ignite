//Pagination constants.
//offset and limit for offline realm pagination
export const REALM_OFFSET = 0;
export const REALM_LIMIT = 15;

//offset and limit for local online api pagination
export const API_OFFSET = 0;
export const API_LIMIT = 30;
